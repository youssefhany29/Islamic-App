Archived Quran debug screens
============================

The debug/probe screens in `debug/` referenced old QPC renderer assets and
old SVG experiment folders that are no longer part of the runtime asset bundle.
Their entry points were removed from the Quran page before moving them here.

The production reader remains in the existing reader container and now renders
Quran-only SVG/WebP pages directly.
