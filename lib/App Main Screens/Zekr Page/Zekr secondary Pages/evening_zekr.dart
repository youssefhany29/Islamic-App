import 'package:flutter/material.dart';

class EveningZekr extends StatelessWidget {
  const EveningZekr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Theme.of(context).colorScheme.background,
    );
  }
}
