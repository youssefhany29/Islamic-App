import 'package:flutter/material.dart';

class MorningZekr extends StatelessWidget {
  const MorningZekr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Theme.of(context).colorScheme.background,
    );
  }
}
